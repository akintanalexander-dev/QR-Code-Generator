# QR Code Generator

Generate a QR code from a URL in the browser.

## Run locally

```bash
npm start
```

Open [http://localhost:3000](http://localhost:3000), enter an HTTP or HTTPS URL, and select **Generate**. The QR code can be downloaded as a PNG from the result panel.
